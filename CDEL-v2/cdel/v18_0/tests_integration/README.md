# v18.0 Integration Tests

This tier is for replay/stateful fixtures that are intentionally slower than `tests_fast/`.

Current integration tests:
- `test_verifier_recomputes_observation_with_stats_source.py`
