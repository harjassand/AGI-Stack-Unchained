{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://genesis.engine/specs/v1_7r/macro_admit_receipt_v2",
  "title": "Macro admit receipt v2",
  "type": "object",
  "additionalProperties": false,
  "patternProperties": {
    "^x-": {}
  },
  "required": [
    "schema",
    "schema_version",
    "epoch_id",
    "macro_id",
    "macro_def_hash",
    "macro_eval_report_hash",
    "state_ledger_head_hash",
    "verdict",
    "x-meta"
  ],
  "properties": {
    "schema": {"const": "macro_admit_receipt_v2"},
    "schema_version": {"type": "integer", "const": 2},
    "epoch_id": {"type": "string"},
    "macro_id": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "macro_def_hash": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "macro_eval_report_hash": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "state_ledger_head_hash": {"type": "string", "pattern": "^sha256:[a-f0-9]{64}$"},
    "verdict": {"type": "string"},
    "x-meta": {"type": "object"}
  }
}
