# RSI SAS Kernel v15.0 Daemon State

Legacy runtime config root for v15.0 SAS kernel orchestration.

## Current Layout

- `config/`: Configuration root (currently scaffolded).

## Notes

- This path is retained for compatibility with legacy campaign wiring.
- Prefer additive migration (new versioned directories) over in-place contract changes.
